pca <- function(t_set)
{
  #step 1. calculationg the mean of each column
  matrix(1, nrow = nrow(t_set), ncol = 1)
  X_mean <- colMeans(t_set) * matrix()
  
  
  sigma <-
  eigen_vec <-
  D <-
  pca_results <- list('sigma' = sigma, 'eigen_vec' = eigen_vec, 'D' = D)
}